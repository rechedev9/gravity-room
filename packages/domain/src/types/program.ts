import type {
  GenericResults,
  GenericUndoHistory,
  ProgramInstance,
  ProgramInstanceMap,
} from '../schemas/instance';
import type { ProgramDefinition } from '../schemas/program-definition';

export type {
  GenericResults,
  GenericUndoHistory,
  ProgramDefinition,
  ProgramInstance,
  ProgramInstanceMap,
};
