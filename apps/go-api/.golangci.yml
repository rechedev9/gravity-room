version: "2"

run:
  timeout: 2m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - misspell
    - bodyclose

formatters:
  enable: []

linters-settings:
  govet:
    enable-all: true
