{
  "name": "gravity-room",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.3.10",
  "workspaces": [
    "apps/backend/*",
    "apps/frontend/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter web dev",
    "dev:web": "bun run --filter web dev",
    "dev:api": "bun run --filter api dev",
    "build": "bun run --filter web build",
    "build:ci": "VITE_API_URL=${VITE_API_URL:-http://localhost:3001} bun run build",
    "build:web": "bun run --filter web build",
    "typecheck": "bun run --filter web typecheck && bun run typecheck:domain && bun run typecheck:mobile",
    "typecheck:api": "bun run --filter api typecheck",
    "typecheck:domain": "bun run --filter @gzclp/domain typecheck",
    "typecheck:mobile": "bun run --filter mobile typecheck",
    "lint": "bun run --filter web lint && bun run --filter api lint",
    "lint:fix": "bun run --filter web lint:fix && bun run --filter api lint:fix",
    "format:check": "prettier --check .",
    "format:fix": "prettier --write .",
    "test": "bun run --filter web test && bun run test:domain && bun run test:mobile",
    "test:api": "bun run --filter api test",
    "test:domain": "bun run --filter @gzclp/domain test",
    "test:mobile": "bun run --filter mobile test",
    "e2e": "bun run --filter web e2e",
    "e2e:ui": "bun run --filter web e2e:ui",
    "e2e:headed": "bun run --filter web e2e:headed",
    "hooks:validate": "lefthook validate",
    "ci": "bun run hooks:validate && bun run typecheck && bun run typecheck:api && bun run lint && bun run format:check && bun run test && bun run build:ci",
    "context:refresh": "bun run scripts/refresh-claude-context.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.12",
    "eslint": "^9.39.4",
    "lefthook": "^2.1.5",
    "prettier": "^3.8.2",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.58.1"
  },
  "trustedDependencies": [
    "lefthook"
  ],
  "overrides": {
    "axios": ">=1.15.1",
    "vite": ">=7.3.2",
    "rollup": ">=4.59.0",
    "happy-dom": ">=20.8.9",
    "serialize-javascript": ">=7.0.3",
    "picomatch": ">=4.0.4",
    "postcss": ">=8.5.10",
    "brace-expansion": ">=1.1.13",
    "follow-redirects": ">=1.15.12",
    "fast-uri": ">=3.1.1",
    "esbuild": ">=0.25.0",
    "flatted": ">=3.4.0",
    "@tootallnate/once": ">=3.0.1"
  }
}
